# How to set up this project
Node.js is required

- `npm install -g gulp`
- `npm install`

##  Running the project

- `gulp init` (Used to initialize the project libraries, can be run only once)
- `gulp develop`