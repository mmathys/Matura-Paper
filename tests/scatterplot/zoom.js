/**
 * Modul für die Berechnung der Punkte bei Änderung des Zooms.
 */

/**
 * Berechnet die absoluten Koordinaten anhand von den Zoomangaben.
 * @param  {[Number]} x         Koordinate
 * @param  {[Number]} y         Koordinate
 * @param  {[Number]} scale     Skalierungsfaktor
 * @param  {[Array]} translate  Array mit Verschiebung bei der x|y Achse
 * @return {[Array]}            Liefert die Koordinaten des Berechneten Punktes
 *                              zurück.
 */
function viewport(x, y, scale, translate){
  //TODO: funktion.
}
